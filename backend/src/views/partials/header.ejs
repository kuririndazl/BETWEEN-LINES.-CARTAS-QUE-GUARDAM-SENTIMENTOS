<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/public/styles/header.css">
</head>

<body>
    <header class="header-main">
        <div class="header-content">
            <div class="logo">
                <abbr title="Logo do site"><a href="/feed"><img src="/public/img/logo_beetween_letter.png"
                            alt="logo"></a></abbr>
            </div>

            <!-- Links Principais -->
            <nav class="nav-links">
                <a href="/feed" title="Feed"><i class="fas fa-home"></i></a>
                <a href="/escrever" title="Escrever Carta"><i class="fas fa-feather-alt"></i></a>
            </nav>

            <!-- Informações do Usuário (Sempre disponível devido ao middleware) -->
            <div class="user-info-header">
                <% if (user) { %>
                    <a href="/profile" class="profile-link" title="Meu Perfil">
                        <img src="<%= user.profilePictureUrl %>" alt="<%= user.username %>" class="profile-pic">
                    </a>

                    <div class="section-nav">
                        <ul id="user-menu">
                            <li><a href="/profile"><i class="fas fa-user-circle"></i>Perfil</a></li>
                            <li class="separator"></li>
                            <li>
                                <form action="/logout" method="POST" style="display: inline;">
                                    <button type="submit"
                                        style="background: none; border: none; cursor: pointer; color: inherit; font-size: inherit;">
                                        <p id="logout">Sair</p> <i class="fas fa-sign-out-alt"></i>
                                    </button>
                                </form>
                            </li>
                        </ul>
                    </div>
                    <% } else { %>
                        <a href="/login" class="btn-login">Entrar</a>
                        <% } %>
            </div>
        </div>
    </header>
</body>

</html>